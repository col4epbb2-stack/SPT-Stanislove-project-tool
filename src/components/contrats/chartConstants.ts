// Partagé entre ContratsPage (vue portfolio) et ContratsTab (onglet projet) —
// même palette validée que TableauDeBordPage.tsx (node scripts/
// validate_palette.js, skill dataviz), pas de couleur choisie à l'oeil.
export const COLOR_CONSOMME = '#4f46e5' // indigo-600
// Valeur cible : un **repère**, pas une identité de série — même gris neutre
// que TableauDeBordPage (COLOR_CIBLE), pour ne pas la mettre au même rang
// visuel que la consommation qu'elle borne.
export const COLOR_CIBLE = '#9ca3af' // gray-400
export const AXIS_TICK = { fontSize: 11, fill: '#6b7280' }
